from .samples.si_diff import run_si_sample

if __name__ == '__main__':
    """
    Liveness test run on command line. The purse of this run is to show
    diffraction patterns generated by pyemaps for silicon crystal.

    Usage:
        python -m pyemaps -s (--sample)

    """
    import argparse

    parser = argparse.ArgumentParser(description="pyemaps console script options parsing")
    
    parser.add_argument("-s", "--sample", type=bool, nargs="?", const=True, default=False, help="for running sample code", required=False)
    
    #parsing for the arguements
    print(f"Congratulations! you have successfully installed pyemaps!")

    args = parser.parse_args()
    
    if args.sample:
        run_si_sample()
    else:
        print(f"Type: \n   python -m pyemaps -s\nto see a sample diffraction pattern generated by pyemaps!")